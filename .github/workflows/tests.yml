name: Tests
on: [push, pull_request]
permissions:
  contents: read
jobs:
  unit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install -e '.[dev]'
      - run: python -m ruff check src scripts tests
      - run: python -m pytest -m 'not data_quality'
      - run: |
          for script in scripts/marlowe/*.sh scripts/classify/slurm/*.sbatch scripts/youtube/*.sh scripts/youtube/*.sbatch scripts/web_corpora/*.sh scripts/web_corpora/*.sbatch scripts/youtube_filter/*.sh scripts/youtube_filter/*.sbatch scripts/release/*.sbatch; do
            bash -n "$script"
          done
